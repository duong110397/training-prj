<%= content_tag "div", id: "votes-score-#{post.id}", class: "score-vote" do %>
  <% if post.cached_votes_score > 0 %>
    +<%=  post.cached_votes_score %>
  <% else %>
    <%= post.cached_votes_score %>
  <% end %> 
<% end %>
