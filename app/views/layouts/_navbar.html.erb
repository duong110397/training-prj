<% if user_signed_in? %>
  <div class="wrapper">
    <!-- Sidebar  -->
    <nav id="sidebar">
      <div class="sidebar-header">
        <h3>Rails trainning</h3>
      </div>
      <ul class="list-unstyled components">
        <li><%= link_to "Home", root_path%></li>
        <li><%= link_to "All Users", users_path%></li>
        <li><%= link_to "Profile", current_user%></li>
      </ul>
    </nav>

  <!-- Page Content  -->
    <div id="content">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <div class="collapse navbar-collapse navbar-flex" id="navbarSupportedContent">
            <ul class="nav navbar-nav ml-auto">
              <li class="nav-item">
                <%= link_to "Changepassword", edit_user_registration_path%>
              </li>
              <li class="nav-item">
                <a><%= button_to "Logout", destroy_user_session_path, method: :delete, :class => "btn-delete" %></a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
      <div class="containner-content">
<% end %>
